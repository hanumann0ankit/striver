//left rotate by one place
//just copy paste and save the last element then use that to insert the frist element

//left rotate by d places
// reverse(nums.begin(), nums.end());
// reverse(nums.begin() + re, nums.end());
// reverse(nums.begin(), nums.begin() + re);
// 3 times reverse and we arae done

//moves zeros to the end
//two pointer approach

//linear search

//union of two arrays
//kind of merge with two pointers

//intersection two arrays
//two pointer with some other conditions
